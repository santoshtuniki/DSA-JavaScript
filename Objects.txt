An Object is a collection of key-value pairs.

The time complexity of object:

    Insert = O(1)

    Remove = O(1)

    Access = O(1)

    Search = O(n)

    Object.keys() = O(n)

    Object.values() = O(n)

    Object.entries() = O(n)